export default defineNuxtRouteMiddleware(async () =>
  // to, from
  {
    // console.log("命名路由中间件 test", to, from);
    // const resByUsefetch = await fetchUserByUsefetch();
    // console.log('命名路由中间件', resByUsefetch);
  }
);
